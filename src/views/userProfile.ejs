<!DOCTYPE html>
<html lang="es">
<%- include('./partials/head',{
  title : 'Perfil'
}) %>

<body>
  <%- include('./partials/headerNavbar') %>

    <div class="container products-wrapper">
      <div class="row">
        <div class="col-12">
          <h2 class="products-title">Perfil de Usuario</h2>
        </div>
        <div class="col-12">
         <div class="card">
          <div class="card-body">
            <form action="/users/update?_method=PUT" method="POST"
            enctype="multipart/form-data">
            <div class="row product-detail">
              <div class="col-12 col-md-4 d-flex flex-column align-items-center justify-content-between">
                <img src="/images/users/<%= user.avatar ? user.avatar : 'default.png' %>" id="imagePreview" class="avatarImg" alt="imagen de perfil"
                  width="75%" style="object-fit: cover; object-position: top;">
                <div class="form-group">
                  <label for="avatar" class="btn btn-primary fs-4 p-3">Cambiar avatar</label>
                  <input hidden type="file" class="form-input" id="avatar" name="avatar">
                </div>
              </div>
              <div class="col-12 col-md-8 ">
                <div class="row mt-2">
                  <div class="form-group col-12 col-md-6 ">
                    <label class="form-label" for="name">Nombre</label>
                    <input type="text" name="name" class="form-input" id="name"
                      value="<%= user.name %>" >
                  </div>
                  <div class="form-group col-12 col-md-6">
                    <label class="form-label"  for="surname">Apellido</label>
                    <input type="text" name="surname" class="form-input" id="surname"
                      value="<%= user.surname %>" >
                  </div>
                  <div class="form-group col-12 col-md-6">
                    <label class="form-label" for="birthday">Cumpleaños</label>
                    <input type="date" name="birthday" class="form-input" id="birthday" autocomplete="off">
                  </div>
                  <div class="form-group col-12 col-md-6">
                    <label class="form-label" for="email">Email</label>
                    <input type="email" name="email" class="form-input" id="email" autocomplete="off" readonly>
                  </div>
                 
                </div>
                <div class="d-md-flex justify-content-end mt-5 gap-3">
                  <button type="button" class="btn btn-secondary fs-4 p-3"><a href="/" class="text-light">Salir sin
                      guardar</a></button>
                  <button type="submit" class="btn btn-primary fs-4 p-3">Actualizar datos</button>
                </div>
              </div>
            </div>
          </form>
          </div>
         </div>
        </div>
      </div>
  </div>

  <%- include('partials/footer') %>
  <script src="/js/userProfile.js"></script>

</body>

</html>